[package]
name = "aurora-focus"
description = "Aurora UI focus engine — remote/D-pad navigation model for the TV shell"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
