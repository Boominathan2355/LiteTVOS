[package]
name = "aurora-tokens"
description = "Aurora UI design tokens (color, radius, spacing, motion) as Rust constants"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
